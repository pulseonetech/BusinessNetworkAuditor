# Aggregator directories - keep folders, ignore contents
import/*.json
import/*.csv
output/*.json
output/*.csv
output/*.md
output/*.html
output/*.pdf
output/*.log
output/**/*.log
output/M365_Assessment_*/

# M365 Assessment - sensitive config (contains tenant IDs, app IDs, thumbprints)
config/m365-assessment-config.json

# Temporary files
*.tmp
temp/

# PowerShell specific
*.ps1xml

# Claude Code - exclude from main repo
.claude/

# System files
.DS_Store
Thumbs.db

# Session tracking (excluded from repo)
session_activity.log
*.activity

# HIBP API Configuration (contains sensitive API keys)
config/hibp-api-config.json

# Old/reference scripts (not part of the tool)
M365-Assess-Consolidate-fixed.ps1
